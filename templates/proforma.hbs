<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<title>Proforma SKJODT CLIENTE</title>
<style>
body {
    font-family: Arial, sans-serif;
    margin: 40px;
    color: #000;
}
.logo {
    position: absolute;
    top: 20px;
    right: 20px;
    width: 100px;
}
h1 {
    text-align: center;
}
table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}
td, th {
    border: 1px solid #000;
    padding: 8px;
    text-align: left;
}
.no-border td {
    border: none;
    padding: 4px;
}
.qr-container {
    position: fixed;
    bottom: 20px;
    right: 20px;
    text-align: center;
}
.qr-logo {
    width: 60px;
    margin-bottom: 6px;
    display: block;
    margin-left: auto;
    margin-right: auto;
}
.qr-code {
    width: 100px;
    height: 100px;
    max-width: 100px;
    max-height: 100px;
    display: block;
    margin: 0 auto;
    border: 2px solid #1976d2;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.10);
    background: #fff;
}
.qr-lema {
    font-size: 8pt;
    color: #1976d2;
    margin-top: 6px;
    font-weight: bold;
    letter-spacing: 0.5px;
    line-height: 1.2;
}
.qr-url {
    font-size: 7pt;
    color: #1976d2;
    margin-top: 2px;
    font-weight: normal;
    word-break: break-all;
}
</style>
</head>
<body>
  <p>PUREFRUIT CHILE S.A.<br>{{header.direccionempresa}}– Chile<br>RUT : 76 090 854 – 1<br>PLANT USD FDA REG.: 13012619644</p>

  <img src="/assets/logos/surfrut.png" alt="Logo" class="logo">
  <h1>Proforma Invoice - CLIENTE</h1>

  <table class="no-border">
    <tr><td>Date:</td><td>{{header.fecha}}</td></tr>
    <tr><td>Customer:</td><td>{{header.cliente}}</td></tr>
    <tr><td>Invoice Nº:</td><td>{{header.invoice}}</td></tr>
    <tr><td>XP Nº:</td><td>{{header.xp}}</td></tr>
    <tr><td>PO Nº:</td><td>{{header.po}}</td></tr>
    <tr><td>Vessel:</td><td>{{header.vessel}}</td></tr>
    <tr><td>Shipping Date:</td><td>{{header.shipping_date}}</td></tr>
    <tr><td>Port of Loading:</td><td>{{header.port}}</td></tr>
    <tr><td>Final Destination:</td><td>{{header.destino}}</td></tr>
    <tr><td>Container Nº:</td><td>{{header.container}}</td></tr>
    <tr><td>Signature:</td><td>{{header.firma}}</td></tr>
  </table>

  <h3>Product Details</h3>
  <table>
    <tr>
      <th>Quantity (kgs)</th>
      <th>Description of Goods</th>
      <th>Unit Price</th>
      <th>Total Amount</th>
    </tr>
    {{#each productos}}
    <tr>
      <td>{{cantidad}}</td>
      <td>{{descripcion}}</td>
      <td>{{precio_unitario}}</td>
      <td>{{total}}</td>
    </tr>
    {{/each}}
    <tr>
      <td colspan="4" style="text-align: right;">Total DDP Amount: US$ {{total_general}}</td>
    </tr>
  </table>

  <!-- Marca de agua para preview -->
  {{#if isPreview}}
  <div
    style="position: fixed; top: 40%; left: 20%; font-size: 3em; color: #e74c3c; opacity: 0.3; transform: rotate(-20deg); z-index: 9999; pointer-events: none;">
    COPIA NO VERIFICADA
  </div>
  <div
    style="position: fixed; bottom: 10px; left: 0; width: 100%; text-align: center; color: #e74c3c; font-size: 1.1em; font-weight: bold; opacity: 0.8; z-index: 9999;">
    COPIA DE EDICIÓN NO VÁLIDA — Este documento no es oficial hasta ser generado.
  </div>
  {{/if}}


  <!-- QR Code Section -->
  {{#if qrCode}}
  <div class="qr-container">
    <img src="/assets/logos/surfrut.png" alt="Surfrut Logo" class="qr-logo">
    <img src="{{qrCode}}" alt="Código QR de verificación" class="qr-code">
    <div class="qr-lema">
      Documento autenticado por<br>
      <b>DocGen-Server Surfrut</b>
    </div>
    <div class="qr-url">
      <a href="https://generate.surfrut.com" style="color:#1976d2;text-decoration:none;" target="_blank">
        https://generate.surfrut.com
      </a>
    </div>
  </div>
  {{/if}}
</body>
</html>
