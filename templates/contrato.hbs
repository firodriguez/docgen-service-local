<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Contrato PDF</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 40px; }
    h1 { text-align: center; color: #333; }
    .qr-container {
        position: fixed;
        bottom: 20px;
        right: 20px;
        text-align: center;
    }
    .qr-logo {
        width: 60px;
        margin-bottom: 6px;
        display: block;
        margin-left: auto;
        margin-right: auto;
    }
    .qr-code {
        width: 100px;
        height: 100px;
        max-width: 100px;
        max-height: 100px;
        display: block;
        margin: 0 auto;
        border: 2px solid #1976d2;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.10);
        background: #fff;
    }
    .qr-lema {
        font-size: 8pt;
        color: #1976d2;
        margin-top: 6px;
        font-weight: bold;
        letter-spacing: 0.5px;
        line-height: 1.2;
    }
    .qr-url {
        font-size: 7pt;
        color: #1976d2;
        margin-top: 2px;
        font-weight: normal;
        word-break: break-all;
    }
  </style>
</head>
<body>
  <h1>Contrato de Trabajo</h1>
  <p>Nombre: <strong>{{nombre}}</strong></p>
  <p>Cargo: <strong>{{cargo}}</strong></p>
  <p>Fecha de inicio: <strong>{{fecha}}</strong></p>
  <p>Sueldo: <strong>${{sueldo}}</strong></p>
  <!-- QR Code Section -->
  {{#if qrCode}}
  <div class="qr-container">
      <img src="/assets/logos/surfrut.png" alt="Surfrut Logo" class="qr-logo">
      <img src="{{qrCode}}" alt="Código QR de verificación" class="qr-code">
      <div class="qr-lema">
          Documento autenticado por<br>
          <b>DocGen-Server Surfrut</b>
      </div>
      <div class="qr-url">
          <a href="https://generate.surfrut.com" style="color:#1976d2;text-decoration:none;" target="_blank">
              https://generate.surfrut.com
          </a>
      </div>
  </div>
  {{/if}}
</body>
</html>
